import React from "react";

const TrainAgent: React.FC = () => {
    return (
        <div>
            <h1>Train Agent Dashboard</h1>
            <p>Welcome to the API Integration page.</p>
        </div>
    );
};

export default TrainAgent;